import FreeResourcesCourseSlider from "@/components/common/FreeResourcesCourseSlider";

/** Sidebar course promo with hover-to-slide carousel (NCERT books pattern). */
export default function Courses() {
  return <FreeResourcesCourseSlider showBackground />;
}
